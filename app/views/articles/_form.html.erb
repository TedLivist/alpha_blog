<div class="container mt-4">

  <%= render 'shared/errors', obj: @article %>

  <div class="row justify-content-center">
    <div class="col-12">
      <%= form_with(model: @article, class: "shadow p-3 mb-4 rounded bg-secondary", local: true) do |f| %>

        <div class="form-group row">
          <%= f.label :title, class: "col-2 col-form-label text-white" %>
          <div class="col-10">
            <%= f.text_field :title, class: "form-control shadow rounded", placeholder: "Title of article" %>
          </div>
        </div>

        <div class="form-group row">
          <%= f.label :description, class: "col-2 col-form-label text-white" %>
          <div class="col-10">
            <%= f.text_area :description, rows: 10, class: "form-control shadow rounded", placeholder: "Description of article" %>
          </div>
        </div>

        <div class="form-group row justify-content-center">
          <%= f.submit class: "btn btn-outline-light btn-lg" %>
        </div>
      <% end %>
    </div>
    
    <%= button_to 'Go to articles listing', articles_path, method: :get, class: "btn btn-primary btn-lg" %>

  </div>

</div>